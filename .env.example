# Copie para .env na raiz do projeto: é lido pelo docker compose.

POSTGRES_DB=intranet
POSTGRES_USER=intranet
POSTGRES_PASSWORD=troque-esta-senha

MINIO_ROOT_USER=intranet
MINIO_ROOT_PASSWORD=troque-esta-senha

# Gere com: python -c "import secrets; print(secrets.token_urlsafe(64))"
SEGREDO_JWT=troque-este-segredo
MINUTOS_EXPIRACAO_TOKEN_ACESSO=30

# Portas expostas no host (mude se já estiverem em uso)
PORTA_FRONTEND=8080
PORTA_BACKEND=8000
PORTA_BANCO=5432
PORTA_MINIO=9000
PORTA_MINIO_CONSOLE=9001
